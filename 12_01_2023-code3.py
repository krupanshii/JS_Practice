##functions
#addition of 2 numbers
def Add_Number(a,b):
    return a+b #concatenation

a = int(input("Enter the value of 1st number: "))
b = int(input("Enter the value of 2nd number: "))

ans = Add_Number(a,b)
print("The sum is ",ans)

#storing the default values in the function
#initialiation always starts from right side

import pandas
